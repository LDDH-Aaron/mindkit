{
  "name": "mindkit",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "dev": "pnpm --filter @mindkit/server run dev",
    "dev:web": "pnpm --filter @mindkit/web run dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.21.0"
  }
}
