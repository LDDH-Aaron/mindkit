# MindKit 仓库初始化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Initialize the MindKit repository with git submodule, pnpm workspace, packages scaffold, market directory, and CLAUDE.md.

**Architecture:** Monorepo with pnpm workspace. Stello SDK via git submodule included in workspace. packages/server is the core backend (Hono + WS), packages/web is a placeholder.

**Tech Stack:** TypeScript strict, pnpm workspace, Hono, ws, Vitest, tsup

---

## File Map

| Action | Path | Responsibility |
|--------|------|---------------|
| Create | `pnpm-workspace.yaml` | Workspace config including stello submodule packages |
| Modify | `package.json` | Root monorepo scripts, devDependencies |
| Create | `tsconfig.json` | Root TS config (matches stello conventions) |
| Create | `.gitignore` | node_modules, dist, data/ |
| Create | `.env.example` | API key template |
| Create | `CLAUDE.md` | Project instructions for AI assistants |
| Create | `packages/server/package.json` | Server package config with stello deps |
| Create | `packages/server/tsconfig.json` | Server TS config |
| Create | `packages/server/tsup.config.ts` | Build config |
| Create | `packages/server/vitest.config.ts` | Test config |
| Create | `packages/server/src/index.ts` | Entry point (placeholder) |
| Create | `packages/web/package.json` | Web placeholder |
| Create | `market/presets/example/preset.json` | Example preset |
| Create | `data/.gitkeep` | Runtime data directory placeholder |

---

### Task 1: Git submodule setup

**Files:**
- Modify: `.gitmodules` (auto-generated by git)

- [ ] **Step 1: Add stello as git submodule**

```bash
cd ~/Code/MindKit
git submodule add git@github.com:stello-agent/stello.git stello
```

This creates `stello/` directory and `.gitmodules` file.

- [ ] **Step 2: Verify submodule**

```bash
cd ~/Code/MindKit
ls stello/packages/
```

Expected: `core  devtools  server  session  visualizer`

- [ ] **Step 3: Commit submodule**

```bash
cd ~/Code/MindKit
git add .gitmodules stello
git commit -m "chore: add stello as git submodule"
```

---

### Task 2: Root configs (package.json, tsconfig, pnpm-workspace, .gitignore, .env.example)

**Files:**
- Modify: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `tsconfig.json`
- Create: `.gitignore`
- Create: `.env.example`

- [ ] **Step 1: Update root package.json**

```json
{
  "name": "mindkit",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "dev": "pnpm --filter @mindkit/server run dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.21.0"
  }
}
```

- [ ] **Step 2: Create pnpm-workspace.yaml**

```yaml
packages:
  - 'packages/*'
  - 'stello/packages/*'
  - 'stello/packages/devtools/web'
```

- [ ] **Step 3: Create tsconfig.json**

Match stello's root tsconfig:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "resolveJsonModule": true,
    "isolatedModules": true
  }
}
```

- [ ] **Step 4: Create .gitignore**

```
node_modules/
dist/
data/*
!data/.gitkeep
.env
*.tsbuildinfo
```

- [ ] **Step 5: Create .env.example**

```
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
```

- [ ] **Step 6: Run pnpm install to verify workspace resolution**

```bash
cd ~/Code/MindKit
pnpm install
```

Expected: installs successfully, recognizes stello packages in workspace.

- [ ] **Step 7: Commit**

```bash
cd ~/Code/MindKit
git add package.json pnpm-workspace.yaml tsconfig.json .gitignore .env.example pnpm-lock.yaml
git commit -m "chore: root configs — pnpm workspace, tsconfig, gitignore"
```

---

### Task 3: packages/server scaffold

**Files:**
- Create: `packages/server/package.json`
- Create: `packages/server/tsconfig.json`
- Create: `packages/server/tsup.config.ts`
- Create: `packages/server/vitest.config.ts`
- Create: `packages/server/src/index.ts`

- [ ] **Step 1: Create packages/server/package.json**

```json
{
  "name": "@mindkit/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.11",
    "@stello-ai/core": "workspace:^",
    "@stello-ai/session": "workspace:^",
    "hono": "^4.12.9",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.1",
    "tsup": "^8.4.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
```

- [ ] **Step 2: Create packages/server/tsconfig.json**

```json
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"]
}
```

- [ ] **Step 3: Create packages/server/tsup.config.ts**

```typescript
import { defineConfig } from 'tsup'

export default defineConfig({
  entry: ['src/index.ts'],
  format: ['esm'],
  dts: true,
  clean: true,
})
```

- [ ] **Step 4: Create packages/server/vitest.config.ts**

```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    globals: true,
  },
})
```

- [ ] **Step 5: Create packages/server/src/index.ts**

```typescript
/** MindKit 本地服务入口 */
export const VERSION = '0.1.0'
```

Placeholder — actual server bootstrap will come in the next plan.

- [ ] **Step 6: Run pnpm install and verify workspace deps resolve**

```bash
cd ~/Code/MindKit
pnpm install
```

Expected: `@stello-ai/core` and `@stello-ai/session` resolve to workspace packages in `stello/packages/`.

- [ ] **Step 7: Build stello packages (typecheck prerequisite)**

```bash
cd ~/Code/MindKit
pnpm --filter @stello-ai/session run build && pnpm --filter @stello-ai/core run build
```

Expected: both build successfully, producing `dist/` with type declarations.

- [ ] **Step 8: Verify typecheck passes**

```bash
cd ~/Code/MindKit
pnpm --filter @mindkit/server run typecheck
```

Expected: passes with no errors.

- [ ] **Step 9: Commit**

```bash
cd ~/Code/MindKit
git add packages/server/ pnpm-lock.yaml
git commit -m "feat(server): scaffold @mindkit/server package"
```

---

### Task 4: packages/web placeholder

**Files:**
- Create: `packages/web/package.json`

- [ ] **Step 1: Create packages/web/package.json**

```json
{
  "name": "@mindkit/web",
  "version": "0.1.0",
  "private": true
}
```

- [ ] **Step 2: Commit**

```bash
cd ~/Code/MindKit
git add packages/web/
git commit -m "chore(web): add placeholder package"
```

---

### Task 5: Market presets directory + example

**Files:**
- Create: `market/presets/example/preset.json`

- [ ] **Step 1: Create market/presets/example/preset.json**

```json
{
  "name": "Example Assistant",
  "description": "A minimal preset for testing MindKit setup",
  "systemPrompt": "You are a helpful assistant.",
  "forkProfiles": [],
  "skills": [],
  "llm": {
    "model": "claude-sonnet-4-20250514"
  },
  "consolidatePrompt": null,
  "integratePrompt": null
}
```

- [ ] **Step 2: Commit**

```bash
cd ~/Code/MindKit
git add market/
git commit -m "feat(market): add presets directory with example preset"
```

---

### Task 6: data/ directory + .gitkeep

**Files:**
- Create: `data/.gitkeep`

- [ ] **Step 1: Create data/.gitkeep**

Empty file. `.gitignore` already has `data/*` + `!data/.gitkeep` (from Task 2), so this file will be tracked.

- [ ] **Step 2: Commit**

```bash
cd ~/Code/MindKit
git add data/.gitkeep
git commit -m "chore: add data/ directory with gitkeep"
```

---

### Task 7: CLAUDE.md

**Files:**
- Create: `CLAUDE.md`

- [ ] **Step 1: Write CLAUDE.md**

```markdown
# CLAUDE.md — MindKit

> 基于 Stello SDK 的本地对话拓扑产品。

---

## 项目定位

MindKit 是 Stello 的本地落地产品，面向终端用户：
- **多 Space**：每个 Space 是一棵独立的 session 树（StelloAgent 实例）
- **Market**：内置预设配置（skills、fork profiles 等），一键创建 Space
- **本地优先**：文件系统持久化，无需数据库

---

## 仓库结构

- `stello/` — git submodule，指向 stello 仓库，通过 pnpm workspace 引用
- `packages/server/` — 本地后端（Hono + WS），核心包
- `packages/web/` — 前端（占位）
- `market/presets/` — 内置预设配置
- `data/` — 运行时数据（gitignore），每个 Space 一个子目录

---

## Stello 复用

Stello 通过 git submodule + pnpm workspace 引用，未发包：
- `pnpm-workspace.yaml` 包含 `stello/packages/*`
- MindKit 包通过 `workspace:^` 依赖 `@stello-ai/core`、`@stello-ai/session` 等
- 修改 stello 代码后两边立即生效

---

## 技术栈

- TypeScript 严格模式 · pnpm monorepo · Vitest · tsup（ESM）
- 后端：Hono + @hono/node-server + ws
- 存储：文件系统（NodeFileSystemAdapter）

---

## 代码规范

- 遵循 stello 的代码规范：interface 通信、中文注释、KISS、严格模式、无 any
- commit 格式：`feat/fix/docs/test/chore(模块名): 简短中文描述`
- push 前先 `git diff --stat` 确认改动范围

---

## 关键设计决策

1. Space = StelloAgent 实例，文件系统持久化在 `data/spaces/{id}/`
2. Space 懒加载——首次 WS 连接时实例化，不全量启动
3. Preset config.json 是声明式的，运行时由 SpaceFactory 构建完整 StelloAgentConfig
4. API key 通过环境变量注入（.env），不存入 config
5. FileSystemMemoryEngine 需在 MindKit 中实现（stello 目前只有 PG 版）
```

- [ ] **Step 2: Commit**

```bash
cd ~/Code/MindKit
git add CLAUDE.md
git commit -m "docs: add CLAUDE.md project instructions"
```

---

### Task 8: Final verification

- [ ] **Step 1: Full pnpm install**

```bash
cd ~/Code/MindKit
pnpm install
```

- [ ] **Step 2: Verify workspace packages**

```bash
cd ~/Code/MindKit
pnpm ls -r --depth 0
```

Expected: lists `@mindkit/server`, `@mindkit/web`, and all `@stello-ai/*` packages.

- [ ] **Step 3: Verify typecheck**

```bash
cd ~/Code/MindKit
pnpm --filter @mindkit/server run typecheck
```

- [ ] **Step 4: Verify git status is clean**

```bash
cd ~/Code/MindKit
git status
git log --oneline
```

Expected: all committed, clean working tree.
